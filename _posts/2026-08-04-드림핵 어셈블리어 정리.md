---
layout: post
title:  "드림핵 어셈블리어 정리"
date:   2026-08-04
excerpt: "Dream hack"
tag:
- dreamhack
- blog
comments: true
---
- 바이트 오더링: 바이트가 메모리에 저장되는 방식. 빅 엔디안은 가장 왼쪽에 있는 바이트부터 낮은 주소에 저장되고, 리틀 엔디안은 가장 오른쪽에 있는 바이트부터 낮은 주소에 저장됨.

- 빅 엔디안 (Big-endian)
: 가장 왼쪽에 있는(큰) 바이트부터 메모리의 낮은 주소에 저장됨.

- 리틀 엔디안(Little-endian)
: 가장 오른쪽에 있는(작은) 바이트부터 메모리의 낮은 주소에 저장됨.

- 논리 연산
: 참 또는 거짓 값으로 연산을 수행하고 결과로 참(1, true) 또는 거짓(0, false)을 반환함

- 논리 연산자
x || y 
: 둘 중 하나라도 참이면 결과는 참 (OR)

x && y
: 둘 다 참이면 결과는 참. (AND)

!x 
: 참이면 결과는 거짓, 거짓이면 결과는 참 (NOT)

x | y
: 두 비트 중 하나라도 1이면 결과는 1 (OR)
x & y
: 두 비트 모두 1이면 결과는 1 (AND)
x ^ y
: 두 비트가 같으면 결과는 0, 다르면 결과는 1 (XOR)
~x
: 비트가 0이면 결과는 1, 1이면 결과는 0으로 모든 비트를 반전시킴 (NOT)

- 시프트 연산자
비트를 특정 값만큼 왼쪽 혹은 오른쪽으로 이동. n만큼 시프트한 결과는 2n으로 곱하거나 나눈 값과 같음.

- x << n
: 비트를 n만큼 왼쪽으로 이동합니다. 오른쪽 빈 칸은 모두 0으로 채움
== x * 2n


- x >> n
: (산술 시프트) 비트를 n만큼 오른쪽으로 이동
왼쪽 빈 칸은 가장 왼쪽에 있던 비트(MSB)와 동일한 비트 값으로 채움 
(양수는 양수, 음수는 음수로 부호가 유지)
== x / 2n


- x >>> n
:(논리 시프트) 비트를 n만큼 오른쪽으로 이동
왼쪽 빈 칸은 모두 0으로 채움 (음수는 부호가 유지되지 않음)


- 인코딩(Encoding)은 데이터를 특정한 형식으로 변환하는 것. 데이터의 크기를 줄이거나, 컴퓨터가 이해하기 쉽게 변환할 때 사용. 인코딩된 데이터는 디코딩(Decoding)하여 원래의 값을 구할 수 있음.

- 폰 노이만 구조(Von Neumann Architecture)
: 초기 컴퓨터 과학자 중 한명인 폰 노이만(Von Neumann)은 컴퓨터에 연산, 제어, 저장의 세 가지 핵심 기능이 필요하다고 생각함. 근대의 컴퓨터는 연산과 제어를 위해 중앙처리장치(Central Processing Unit, CPU)를, 저장을 위해 기억장치(Memory)를 사용. 그리고 장치간에 데이터나 제어 신호를 교환할 수 있도록 버스(Bus)라는 전자 통로를 사용함.

- 명령어 집합 구조(Instruction Set Architecture, ISA)란 CPU가 해석하는 명령어의 집합을 의미. 프로그램은 기계어로 이루어져 있는데, 프로그램을 실행하면 이 기계어로된 명령어들을 CPU가 읽고, 처리함. ISA는 기계어를 CPU가 어떻게 해석하고 처리할지 방법을 정의함.



- 코드 세그먼트(Code Segment)는 실행 가능한 기계어 코드가 위치하는 영역으로, 텍스트 세그먼트(Text Segment)라고도 부름. 해당 영역은 실행할 수 있어야 하므로 읽기 권한과 실행 권한이 주어짐.

- 데이터 세그먼트(Data Segment)에는 컴파일 시점에 값이 정해진 전역 변수 및 전역 상수들이 위치함. CPU가 이 세그먼트의 데이터를 읽을 수 있어야 하므로, 읽기 권한이 부여됨.

- BSS 세그먼트(BSS Segment, Block Started By Symbol Segment)는 컴파일 시점에 값이 정해지지 않은 전역 변수가 위치하는 메모리 영역임. 여기에는 개발자가 선언만 하고 초기화하지 않은 전역변수 등이 포함됨. 이 세그먼트의 메모리 영역은 프로그램이 시작될 때, 모두 0으로 값이 초기화됨. 이런 특성 때문에 C 코드를 작성할 때, 초기화되지 않은 전역 변수의 값은 0이됨.

- 스택 세그먼트(Stack Segment)는 프로세스의 스택이 위치하는 영역. 함수의 매개변수나 지역 변수같은 임시 변수들이 실행 중에 이곳에 저장됨. 해당 세그먼트는 NX (Non-Executable) 보호기법이 나오기 전까지 실행 권한이 존재했지만, 쉘코드 (Shellcode)를 이용해 스택에 기계 코드를 넣어서 해커가 해당 스택 영역을 호출하는 기법이 나오면서 컴파일 시 기본적으로 NX 보호기법을 활성화 시켜 실행 권한을 없앴기 때문에 해당 세그먼트에는 읽기 권한과 쓰기 권한이 주어짐.

- 힙 세그먼트(Heap Segment)는 힙 데이터가 위치하는 세그먼트. 스택과 마찬가지로 실행중에 동적으로 할당될 수 있으며, 리눅스에서는 스택 세그먼트와 반대 방향으로 자람.


- 목적 파일(Object file)이란 컴파일이나 어셈블 과정에서 소스 코드로부터 생성된 기계 코드(Machine code)를 포함하고 있는 파일을 뜻함. 목적 파일에는 각 함수나 전역 변수에 해당하는 기계어, 명령어 그리고 아직 연결되지 않은 외부 심볼 등이 포함되어 있음. 파일별로 목적 파일을 만들어 두면 여러 모듈을 독립적으로 컴파일할 수 있어 전체 빌드 시간을 크게 단축할 수 있으며, 재사용성과 효율성이 크게 향상됨.

- 링커(Linker)는 여러 목적 파일들을 하나로 결합해 최종적으로 실행 가능한 파일을 생성함. 이 과정에서 각 목적 파일 사이 함수 호출이나 변수 참조 같은 심볼 이름을 연결하고, 각 목적 파일에서 사용하던 상대 주소를 실제 메모리 주소로 재배치하는 등의 과정을 거침. 이렇게 완성된 실행 파일은 운영체제가 직접 로드해 실행할 수 있는 독립적인 바이너리가 됨.



## 명령어

- 명령 코드
데이터 이동(Data Transfer)
: mov, lea
산술 연산(Arithmetic)
: inc, dec, add, sub
논리 연산(Logical)
: and, or, xor, not
비교(Comparison)
: cmp, test
분기(Branch)
: jmp, je, jg
스택(Stack)
: push, pop
프로시져(Procedure)
: call, ret, leave
시스템 콜(System call)
: syscall

add: 덧셈
sub: 뺄셈
mul, imul: 곱셈
div, idiv: 나눗셈

스택: push, pop
프로시저: call, leave, ret
시스템콜: syscall


## 스택(Stack)이란?

스택은 자료 구조의 한 종류로, LIFO (Last In, First Out, 후입선출) 방식으로 동작하는 구조. 즉, 가장 나중에 삽입된 데이터가 가장 먼저 꺼내지는 구조를 가짐. 데이터를 추가할 때마다 메모리 주소가 감소하며, 데이터를 제거하면 다시 증가하는 특성이 있음.

- push
: push는 스택에 값을 저장하는 명령어. push 명령어를 실행하면 rsp가 감소하면서 스택에 값이 저장됨.

- pop
: pop은 스택에서 값을 꺼내는 명령어. pop을 실행하면 rsp가 증가하면서 스택에 저장되어 있던 값을 꺼낼 수 있음.

- call
: 먼저 함수를 호출할 때 사용하는 call 명령어.

- leave
: 다음은 프로시저가 반환되기 전, 스택 프레임(Stack Frame)을 정리하는 leave 명령어.

## 스택프레임(Stack Frame)이란?
: 스택은 함수별로 자신의 지역변수 또는 연산과정에서 부차적으로 생겨나는 임시 값들을 저장하는 영역. 이 스택 영역을 아무런 구분 없이 사용하게 된다면, 서로 다른 두 함수가 같은 메모리 영역을 사용할 수 있게 됨.

- 커널 모드
: 커널 모드(Kernel Mode)는 운영체제가 전체 시스템을 제어하기 위해 시스템 소프트웨어에 부여하는 권한. 커널 모드에선 모든 메모리 영역에 접근할 수 있고, 하드웨어에 직접적으로 접근할 수 있음. 파일시스템, 입력/출력, 네트워크 통신, 메모리 관리 등 모든 저수준의 작업은 사용자 모르게 커널 모드에서 진행됨.

- 사용자 모드
: 사용자 모드(User Mode)는 운영체제가 사용자에게 부여하는 권한으로, 접근할 수 있는 메모리 영역과 권한이 매우 한정되어 있고, 하드웨어에 직접적으로 접근할 수 없음. 브라우저를 이용하여 드림핵을 보거나, 유튜브를 시청하는 것, 게임을 하고 프로그래밍을 하는 것 등은 모두 유저 모드에서 이루어짐. 유저 모드에서는 해킹이 발생해도, 해커가 유저 모드의 권한까지 밖에 획득하지 못하기 때문에 해커로부터 커널의 막강한 권한을 보호할 수 있음.

- 시스템 콜
: 시스템 콜(System Call, syscall)은 유저 모드에서 커널 모드의 시스템 소프트웨어에게 어떤 동작을 요청하기 위해 사용. 소프트웨어 대부분은 커널의 도움이 필요함. 예를 들어, 사용자가 cat flag라는 리눅스 명령어를 실행하면, flag라는 파일을 읽어서 사용자의 터미널 화면에 출력해주어야 함. 그런데 flag는 파일 시스템에 존재하므로 이를 읽으려면 파일시스템에 접근할 수 있어야 함. 유저 모드에서는 이를 직접 할 수 없으므로 커널이 도움을 줘야 함. 여기서, 도움이 필요하다는 요청을 시스템 콜이라고 함. 유저 모드의 소프트웨어가 필요한 도움을 요청하면, 커널이 요청한 동작을 수행하여 유저에게 결과를 반환함.

- x86 호출 규약
1. cdecl 
: cdecl은 리눅스의 gcc가 x86 바이너리를 컴파일할 때 일반적으로 사용하는 호출 규약으로, 레지스터 없이 스택만으로 인자를 전달. 또한, 인자를 전달하기 위해 사용한 스택을 호출자가 정리하는 특징이 있음.

2. stdcall 
: stdcall (standard call)은 윈도우 API (Application Programming Interface)에서 기본적으로 사용되는 함수 호출 규약. 윈도우 운영체제에서 시스템 호출(API 호출)의 효율성을 높이기 위해 cdecl 호출 규약을 변형하여 stdcall 호출 규약을 만들었으며, 인자 전달을 위해 확장된 스택 정리를 호출자가 아닌 피호출자가 한다는 것이 가장 큰 차이점.

3. fastcall 
: fastcall은 함수 호출 시 일부 인자를 레지스터로 전달하여 속도를 향상시키기 위해 도입된 규약.

4. thiscall 
: thiscall은 x86에서 사용되는 C++ 클래스 멤버 함수를 위한 함수 호출 규약. 멤버 함수에서 사용하는 this 포인터를 ecx로 넘기고, 나머지 인자들은 모두 스택을 통해 전달함.

5. SYSV
: 리눅스는 SYSTEM V(SYSV) Application Binary Interface(ABI)를 기반으로 만들어짐. SYSV ABI는 ELF 포맷, 링킹 방법, 함수 호출 규약 등의 내용을 담고 있음.

## 터미널의 정규(Canonical) 모드와 비정규(Non-canonical) 모드

- 정규 모드(Canonical Mode)는 터미널을 사용하면 기본적으로 설정되는 모드. 이용자의 모든 입력이 줄 단위로 처리되며, 각 줄은 개행 문자, EOF (End Of File), EOL (End Of Line) 등으로 구분됨. 즉, 일반적인 상황에서는 이용자가 엔터 키를 입력하기 전까지는 문자열이 전달되지 않음. 일반적으로 엔터 키를 누르는 순간 명령어가 실행되는 이유는 터미널이 정규 모드 상태이기 때문.

- 비정규 모드(Non-canonical Mode)는 줄 단위로 입력을 처리하는 것이 아닌, 바이트 단위로 입력을 처리. vi 에디터의 입력 모드에서는 개행 문자를 입력해도 프로그램이 종료되거나 명령어로 인식해 실행되지 않으며, 모든 입출력이 문자 단위로 이루어짐. 이용자의 입력이 바로 터미널에 출력 및 처리되는 이유도 터미널이 비정규 모드 상태이기 때문.

- open, close: 파일을 열고 닫을 때 사용. open 시스템 콜은 파일 열기에 성공했을 경우 파일 디스크립터 값을 반환하며, 실패할 경우 -1을 반환. close 시스템 콜은 파일 디스크립터를 인자로 받아 파일을 닫을 때 사용.


- lseek: 파일 오프셋 이동에 사용. 첫 번째 인자로 파일 디스크립터를, 두 번째 인자로 오프셋을, 세 번째 인자로 옵션을 받음. 오프셋과 옵션에 따라 파일 포인터의 위치를 변경.

- ioctl: 입출력 제어(I/O Control)의 약자로, read나 write와 같은 일반적인 입출력으로 처리할 수 없는, 장치 드라이버와 이용자 공간 프로그램 간의 특수한 입출력을 제어할 때 주로 사용. 


- termios 구조체
: termios는 POSIX 계열 운영체제에서 터미널의 동작 모드를 제어하기 위해 사용하는 구조체. C 언어에서 tcgetattr()를 통해 현재 termios 구조체의 값을 가져올 수 있고, tcsetattr()를 통해 termios 구조체의 값을 수정할 수 있음.
